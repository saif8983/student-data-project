
import { createTheme} from '@mui/material/styles';
const themeOfLogo = createTheme({
    typography: {
      fontFamily: ['IBM Plex Sans, sans-serif'].join(','),
    }
  })
  
  export default themeOfLogo